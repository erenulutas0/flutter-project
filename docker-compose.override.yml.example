# Example override file for local development
# Copy this to docker-compose.override.yml and customize

version: '3.8'

services:
  backend:
    environment:
      # Override for local development
      LOG_LEVEL: DEBUG
      SPRING_JPA_SHOW_SQL: "true"
    
    # Mount local code for hot reload (development only)
    # volumes:
    #   - ./backend/src:/app/src

  frontend:
    # Mount local code for hot reload (development only)
    # volumes:
    #   - ./flutter_app/lib:/usr/share/nginx/html/lib


